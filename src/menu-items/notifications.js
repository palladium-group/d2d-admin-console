import { IconBell } from '@tabler/icons';

const notifications = {
  id: 'notifications',
  title: 'Notifications',
  type: 'group',
  icon: IconBell,
  url: '/notifications'
};

export default notifications;
